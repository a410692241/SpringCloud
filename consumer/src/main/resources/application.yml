server:
  port: 8800
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
    fetch-registry: false
    register-with-eureka: false
  instance:
    hostname: consumer
spring:
  application:
    name: consumer